[project]
name = "discord-deleter"
version = "0.1.0"
description = "Mass-delete your Discord messages before deleting your account."
requires-python = ">=3.10"
dependencies = [
    "aiohttp>=3.9",
    "textual>=0.60",
]

[tool.uv]
package = false
